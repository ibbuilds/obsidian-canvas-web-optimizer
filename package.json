{
  "name": "obsidian-canvas-web-optimizer",
  "version": "0.1.0",
  "description": "Optimize web pages in Obsidian Canvas with cached previews and smarter webview resource management.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "format": "biome format --write src package.json manifest.json",
    "lint": "biome lint src",
    "check": "biome check src && tsc --noEmit --skipLibCheck",
    "build": "pnpm run check && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": [
    "obsidian",
    "canvas",
    "web",
    "webview",
    "performance",
    "optimizer"
  ],
  "author": "ibbuilds",
  "license": "MIT",
  "devDependencies": {
    "@biomejs/biome": "2.5.13",
    "@types/node": "^16.11.6",
    "esbuild": "0.17.3",
    "obsidian": "latest",
    "typescript": "4.7.4"
  },
  "dependencies": {
    "monkey-around": "^3.0.0"
  }
}
